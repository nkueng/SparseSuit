debug: True
defaults:
 - experiment: baseline
 - hyperparams: default
 - override hydra/job_logging: disabled
 - override hydra/hydra_logging: disabled
# - override hydra/launcher: submitit_local
 - override hydra/launcher: submitit_slurm


hydra:
#  setup for slurm
  launcher:
    submitit_folder: null
    timeout_min: 1  # just to check array, def: 300
    cpus_per_task: 1
    gpus_per_node: 1
    tasks_per_node: 1
    mem_gb: 2
    nodes: 1
    additional_parameters: {"mail-user": "nickueng@gmail.com", "mail-type": "BEGIN,END,FAIL,TIME_LIMIT_80"}
    array_parallelism: 256
    setup: null

  run:
    dir: .
  output_subdir: null
  sweep:
    dir: .
    subdir: .
