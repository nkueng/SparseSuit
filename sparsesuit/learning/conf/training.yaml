debug: False
gpu: True
traintime_noise: True
defaults:
  - experiment: SSP_baseline
  - hyperparams: default
  - override hydra/job_logging: disabled
  - override hydra/hydra_logging: disabled
  - _self_

hydra:
  run:
    dir: .
  output_subdir: null
  sweep:
    dir: .
    subdir: .
